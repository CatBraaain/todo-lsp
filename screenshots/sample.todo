Inbox:
    this line should be gray @done(2024-01-01)
    this line should be gray @done(2024-01-01)
    the two gray lines above should be hidden when comment folding is applied
    the contents of every top-level heading below should be hidden when level-1 folding is applied
    Project:
        this line should be plain and the tag light purple @priority(high)
        this line should be gray @done
    dummy
Dates:
    the start tag should be yellow @start(2024-01-01)
    the start tag should be light purple @start(2099-12-31)
    the start tag should be red and underlined @start(not-a-date)
    the start tag should be red and underlined @start
    the due tag should be red @due(2024-01-01)
    the due tag should be light purple @due(2099-12-31)
    the due tag should be red and underlined @due(not-a-date)
    the due tag should be red and underlined @due
Repeat:
    the repeat tag should be orange @repeat(0 0 * * *)
    the repeat tag should be red and underlined @repeat(not-a-cron)
    the repeat tag should be red and underlined @repeat
Tags:
    the queue tag should be yellow @queue(1)
    the queue tag should be light purple @queue(2)
    this line should be gray and italic @cancelled
    this line should be gray @hide
Decorations:
    **bold** *italic* `code` <https://example.com> should render as red bold, italic, orange code, and a link
Archive:
    this line should be plain
