# Todo — SPEC

VS Code 拡張「Todo」が `.todo` / `.tasks` ファイルに提供する、ユーザーが観測できる振る舞いの仕様書。

## 対象と適用範囲

- 対象: VS Code 拡張 Todo が言語ID `todo` のファイルへ提供する機能すべて
- 利用者: `.todo` / `.tasks` ファイルでタスク管理をするユーザー
- 適用範囲: ファイル関連付け、エディタ規定値、構文、診断、アウトライン、折りたたみ、表示、コマンド、フォーマット、アーカイブ、リピート、設定

## 利用環境

| 条件 | 結果 |
|---|---|
| `.todo` または `.tasks` ファイルを開く | 文書を `Todo` 言語として扱い、支援機能を有効にする。 |
| Windows x64 または Linux x64 でローカルの Todo 文書を開く | 文書の診断、アウトライン、折りたたみ、semantic highlighting を提供する。 |
| それ以外のプラットフォームで Todo 文書を開く | `todo-lsp: unsupported platform <platform>/<arch>. Ship a matching binary under bin/.` をエラーとして返す。 |

## 用語

| 用語 | 定義 |
|---|---|
| 見出し行 | 本文（空でない）の直後に `:` を置き、その後が空、または空白に続くタグ列である行 |
| タスク行 | 見出し行以外の行 |
| タグ | タグ列を構成する `@name` または `@name(引数)` のトークン |
| タグ列 | 行末に空白区切りで連続するタグ。見出し行では `:` の直後、タスク行では本文の直後に置く |
| 灰色行 | `@done` / `@cancelled` / `@hide` のいずれかを持つ行 |
| インデントレベル | 行頭の字下げ量。1レベル = スペース4個、またはタブ1個（タブは直前のスペース3個までと合わせて1レベルと数える） |
| ブロック | ある行と、それより深いインデントの子孫行のまとまり。親子関係はインデントレベルで決まる |
| トップレベルブロック | ルート直下（インデントレベル0）の行を先頭とするブロック |
| 選択行 | カーソルまたは選択範囲が含む行。複数選択では全範囲の和集合 |
| タスクテキスト | 行からタグと空白だけを除いた語句をスペース1つで連結した文字列 |
| 実行日 | コマンドを実行した日の `YYYY-MM-DD` |

## ファイルと言語

| 項目 | 振る舞い |
|---|---|
| ファイル関連付け | `.todo` / `.tasks` 拡張子のファイルを言語ID `todo` で開く |
| 言語名 | Todo / Tasks |
| 括弧の自動入力 | `{` `[` `(` `` ` `` の入力で閉じ側を自動入力する |
| 囲みペア | `{}` `[]` `()` `` ` `` `*` を選択範囲の囲みペアとする |

言語ID `todo` のファイルのエディタ規定値: semantic highlighting 有効、タブサイズ 4、括弧ペア彩色なし、括弧対応の強調なし、出現箇所ハイライトなし、選択ハイライトなし、sticky scroll なし、折りたたみハイライトなし。

## 文書構造の境界

Todo文書は行の列であり、空行と空白だけの行は構造に含まれない。構造に含まれる各行は見出し行またはタスク行である。子要素は文書中の順序とインデント階層を保つ。

```todo
# note
send to a@b
literal @ here
```

- `# note` はコメントではなく、本文が `# note` のタスク行である。
- `send to a@b` と `literal @ here` の `@` は行末タグ列を構成しないため、本文の一部である。

### タグの構文

- `name` は、空白と `(` を含まない1文字以上の文字列。
- `引数` は `(` と `)` で囲む任意の文字列。空にできるが、`)` は含めない。

## 構文要素

| 要素 | 記法 | 意味 |
|---|---|---|
| 見出し | `テキスト:` | 子行をまとめる。ネストできる |
| タスク | 見出し以外の行 | 1行が1タスク |
| 完了タグ | `@done` / `@done(日付)` | 完了。行を灰色行にする |
| 取消タグ | `@cancelled` / `@cancelled(日付)` | 取消。行を灰色行にする |
| 非表示タグ | `@hide` | 行を灰色行にする |
| 期限タグ | `@start(日付)` / `@due(日付)` | 開始日・期限日。日付の新旧で表示が変わる。日付は UTC の `YYYY-MM-DD` または `YYYY-MM-DD HH:mm` |
| キュータグ | `@queue(番号)` | 優先順位 |
| 状態タグ | `@waiting` / `@pending` | 待ち・保留の状態 |
| 繰り返しタグ | `@repeat(cron式)` | 定期タスクの定義。cron式は5フィールドで、`L` `#` `W` 拡張を受理する |
| イタリック | `*語句*` | 語句をイタリック表示する |
| 太字 | `**語句**` | 語句を太字表示する |
| コード | `` `語句` `` | 語句をコード表示する |
| URL | `<https://…>` `<http://…>` `<ftp://…>` | リンク表示する |

## 完成形

次の `sample.todo` を開いたとき、構文エラーはなく、診断を表示しない。

```todo
Inbox:
  buy milk
  call mom @done(2024-01-01)
  Project:
    draft spec @priority(high)
    review @done
  wrap up
Archive:
  old task
```

## 診断

文書を開く、または全文を変更すると、現在の文書に対する診断を更新する。構文エラーがない文書ではエラー表示を出さない。文書を閉じると、その文書の診断表示を消す。

```todo
@done(
@done
:
```

タグだけの行と本文のない見出し行を含め、エラー範囲に ERROR 診断を表示する。メッセージは `syntax error` または `missing syntax element` である。

## 表示

### 行単位の色付け

| 対象 | 表示 |
|---|---|
| `@done` / `@hide` を持つ行 | 行全体を灰色（#666666） |
| `@cancelled` を持つ行 | 行全体を灰色（#666666）、イタリック |
| `Archive:` 見出し行 | 行全体を灰色（#666666） |

### インラインの色付け

| 対象 | 表示 |
|---|---|
| 見出しの本文（`:` の前） | 水色（#66d9ef）、太字 |
| 見出しの `:` | 太字 |
| `@queue(1)` | 黄色（#F9F871） |
| `@queue(2)`、`@queue(3)`、その他のタグ | 淡紫（#ACA7CB） |
| `**太字**` | 赤系（#D57F6D）、太字 |
| `*イタリック*` | イタリック |
| `` `コード` `` | 橙（#FAB565） |

### 日付・cron の色付け

| タグの状態 | `@start` | `@due` | `@repeat` |
|---|---|---|---|
| 日時が現在より過去 | 黄色（#F9F871） | 赤（#FF0000） | ― |
| 日時が現在より未来 | 淡紫（#ACA7CB） | 淡紫（#ACA7CB） | ― |
| 日付として解釈不能、または引数なし | 赤字＋下線 | 赤字＋下線 | ― |
| cron式として有効 | ― | ― | 橙（#FAB565） |
| cron式として解釈不能、または引数なし | ― | ― | 赤字＋下線 |

### 適用規則

行全体の装飾、タグ、インライン装飾とURLに対し、次の優先順位で最初に一致する装飾を適用する。

1. `Archive:` 見出し行
2. `@done` を持つ行
3. `@cancelled` を持つ行
4. `@hide` を持つ行
5. 見出し行
6. 通常行

- 灰色行と `Archive:` 見出し行では、行内のタグやインライン装飾を個別表示しない。
- 見出し行では後続のタグを個別表示し、インライン装飾は個別表示しない。
- 閉じていないインライン装飾やURLは個別表示しない。

## アウトライン

アウトラインは文書構造の階層をツリーとして表示する。表示名は見出しは `:` の前の本文、タスクはタグを除いた本文とする。

## コマンド

既定キーは `todo` ファイルの編集フォーカス中（読み取り専用を除く）で有効。すべてコマンドパレットからも実行できる。

| 既定キー | コマンドID / パレット名 | 振る舞い |
|---|---|---|
| Alt+D | `todo-language.toggleDone` / Toggle Line Done | 選択行に `@done(実行日)` をトグル |
| Alt+C | `todo-language.toggleCancelled` / Toggle Line Cancelled | 選択行に `@cancelled(実行日)` をトグル |
| Alt+S | `todo-language.toggleStart` / Toggle Line Start | 選択行に `@start(実行日)` をトグル |
| Alt+Shift+D | `todo-language.toggleDue` / Toggle Line Due | 選択行に `@due(実行日)` をトグル |
| Alt+Q | `todo-language.toggleQueue` / Toggle Line Queue | 選択行に `@queue(次番号)` をトグル |
| Alt+Shift+Q | `todo-language.toggleQueueUnshift` / Toggle Line Queue Unshift | 選択行に先頭キューをトグル |
| Alt+W | `todo-language.toggleWaiting` / Toggle Line Waiting | 選択行に `@waiting` をトグル |
| Alt+P | `todo-language.togglePending` / Toggle Line Pending | 選択行に `@pending` をトグル |
| Alt+H | `todo-language.toggleHide` / Toggle Line Hide | 選択行に `@hide` をトグル |
| Alt+R | `todo-language.toggleRepeat` / Toggle Line Repeat | 選択行に `@repeat(0 0 * * *)` をトグル |
| Tab | `todo-language.indent` / Indent Lines | 選択行のインデントを +1 レベル |
| Shift+Tab | `todo-language.dedent` / Dedent Lines | 選択行のインデントを −1 レベル |
| （キーなし） | `todo-language.repeatTasks` / Repeat Tasks | 繰り返しタスクを生成（§リピート） |
| Alt+A | `todo-language.archive` / Archive Task Blocks | 選択ブロックをアーカイブ（§アーカイブ） |
| Alt+Shift+A | `todo-language.unarchive` / Unarchive Task Blocks | 選択ブロックを復帰（§アーカイブ） |

### タグトグルの共通規則

1. 操作対象は選択行すべて。
2. 選択行のうちタグを持たない行があるとき、それらの行の末尾にタグを追加する。
3. 選択行すべてがタグを持つとき、全選択行からそのタグを除去する。
4. `@done` / `@cancelled` を追加するとき、対象行から `@done` `@cancelled` `@queue` `@waiting` `@pending` を除去してから追加する。
5. 追加・除去の後、対象行はフォーマット規則（§フォーマット）で整形される。

### キュー番号

- 追加時の番号は、灰色行を除く `@queue` 付き行の数 + 1。
- Queue Unshift は `@queue(0)` を追加し、再採番の結果 先頭（1）になる。
- 再採番は `@queue` のトグル実行ごとに行い、文書内の全 `@queue(n)` の番号を n の昇順での順位に書き換える。同じ n は同じ番号になる。
  例: `@queue(0)` `@queue(2)` `@queue(5)` `@queue(5)` → `@queue(1)` `@queue(2)` `@queue(3)` `@queue(3)`

### インデント

- Indent Lines は選択行のインデントを +1 レベル、Dedent Lines は −1 レベルにする。レベルは 0 を下限とし、1レベルをスペース4個で書き込む。

## フォーマット

「ドキュメントのフォーマット」（Shift+Alt+F）と、タグトグル・Repeat Tasks・Archive・Unarchive の実行後に適用される。

1. 各行のインデントを「親行のレベル + 1」× スペース4個へ正規化する。
2. 行内のタグ・テキストの間をスペース1個にそろえ、行頭・行末の余分な空白を取り除く。
3. 連続する空行を1行にまとめ、文書先頭の空行を削除し、文書末尾に改行を置く。
4. トップレベルの見出しブロックの前後に空行を置く。

## 折りたたみ

子要素を持つ各見出しは、見出し行から子要素の最終行までを折りたたみ領域として折りたためる。入れ子の見出しは個別に折りたためる。

`sample.todo`（§完成形）では次の範囲を折りたためる。

```text
Inbox:   1行目から7行目
Project: 4行目から6行目
Archive: 8行目から9行目
```

## アーカイブ

アーカイブ対象は「選択行を含み、非空の行すべてが灰色行」のトップレベルブロック。

- Archive（Alt+A）: 対象ブロックを元の位置から除き、ルートレベルの `Archive:` 見出しの末尾に移動する。`Archive:` 行が文書にないときは文末に作成する。移動した行は整形される。
- Unarchive（Alt+Shift+A）: `Archive:` 配下の対象ブロックを文書末尾へ移動する。`Archive:` 行がないときは文書を変えない。

## リピート

`@repeat(cron式)` を持つ行が繰り返しタスク定義。タスクテキストを `/` で区切ると `親見出し/…/タスク名` となり、配置先を指定できる。

Repeat Tasks（`todo-language.repeatTasks`）の振る舞い:

1. cron式が有効な `@repeat` 行をすべて処理する。
2. 各定義について、cron式の現在より前の直近の日時 prev を求める。
3. 定義行が `@start(日付)` を持ち、その日付が prev より未来のとき、その定義の処理を終える。
4. 配置先を `/` パスの順に解決する。各親名を、上のレベルの子のうちタスクテキストが親名を含む行から探し、ないときは親名の行をその階層に作る。
5. 配置先の末尾に `タスク名 @start(prev)` の行を追加する。prev の時刻が 00:00 なら日付は `YYYY-MM-DD`、それ以外は `YYYY-MM-DD HH:mm`。
6. 配置先に「同じタスク名と同じ `@start` を持つ行」があるとき、追加をせずその定義の処理を終える。
7. 全定義の処理の後、文書全体を整形する。

自動リピートは、設定 `repeatTask.autoRepeat` が true かつアクティブエディタが `todo` のとき、次のタイミングで Repeat Tasks を実行する。

- 拡張の起動時
- アクティブエディタの切替から約 0.5 秒後
- 毎分 0 秒

## 設定

| 設定 | 型 | 既定 | 振る舞い |
|---|---|---|---|
| `todo-language.repeatTask.autoRepeat` | 真偽値 | true | 自動リピートの実行 ON/OFF |
